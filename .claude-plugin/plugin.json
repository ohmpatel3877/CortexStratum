{
  "name": "ai-memory-core",
  "version": "1.0.0",
  "description": "Central mem0 memory architecture for AI agent persistence — 68-tool MCP server, skill router, task orchestrator, verifier middleware, and cross-session memory continuity for OpenCode and Claude Code.",
  "author": {
    "name": "Ohm Patel",
    "url": "https://github.com/ohmpatel3877"
  },
  "license": "MIT",
  "keywords": [
    "mem0",
    "memory",
    "mcp",
    "agent-memory",
    "skill-router",
    "task-orchestrator",
    "verifier",
    "opencode",
    "persistence"
  ],
  "harnesses": ["opencode", "claude-code", "cursor"],
  "tools": {
    "mcp_servers": [
      {
        "name": "ai-memory-core",
        "description": "68-tool MCP server exposing xTrace error tracking, DTrace architecture decisions, Skill Router, Output Condenser, Goal Registry, Commitment Checker, Verifier Middleware, and multi-module AI generation (art, literature, coder, devops, audio, sensory, game-dev)",
        "command": "python",
        "args": ["scripts/tools-mcp-server.py"],
        "working_dir": "${PROJECT_ROOT}"
      }
    ],
    "scripts": {
      "seed": "node scripts/seed-memories.js",
      "dream": "node scripts/consolidate.js",
      "status": "node scripts/check-status.js",
      "analyze": "python scripts/task-analyzer.py"
    }
  },
  "skills": [
    {
      "name": "task-orchestrator",
      "description": "Auto-decompose complex tasks into parallel subagent workstreams",
      "path": "skills/task-orchestrator/SKILL.md"
    }
  ],
  "install": {
    "requirements": ["node >= 18", "python >= 3.10"],
    "steps": [
      "npm install",
      "cp .env.example .env (or set MEM0_API_KEY)",
      "node scripts/seed-memories.js"
    ]
  }
}
