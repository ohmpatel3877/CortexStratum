# ai-memory-core — Environment Configuration
# Fully local — no API keys needed. Memory stays on your machine.

MEM0_PROJECT=ai-memory-core

# Optional: Customize memory retention (days)
# MEM0_RETENTION_DAYS=365
